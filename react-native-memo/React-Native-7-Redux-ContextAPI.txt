1. React-Native and useContext
1-1. 웹의 useContext 사용과 다를것이 없다.

store 폴더, context 파일 생성 => context에서 createContext 객체 + export => provider을 리턴하는 함수 생성
=> children과 state 구현 => state을 사용하는 모든 컴포넌트 덮기 => state을 사용할 컴포넌트에서 useContext를 사용해서 
상태를 사용하면 된다.


2. Redux-Toolkit
